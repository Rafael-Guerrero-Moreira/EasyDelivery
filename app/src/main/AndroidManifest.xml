<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easydelivery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyDeliver">
        <activity android:name=".module.CreateProduct"></activity>
        <activity android:name=".module.CrateAcountDelivery" />
        <activity android:name=".module.CreateAucontforBuisnes" />
        <activity android:name=".UserType" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".menu.Search" />
        <activity android:name=".menu.Category" />
        <activity android:name=".menu.Store" />
        <activity android:name=".menu.SettingUser" />
        <activity android:name=".login" />
        <activity android:name=".module.CreateAcount" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VerifyToken">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>